# write-fonts

This crate contains types for creating and editing font-files.

## Features

### `tables` (default)

Enables support for owned types representing various tables and records in the
OpenType specification, along with validation and compilation logic for them.
